package com.cg.homeLoan.repo;


import org.springframework.stereotype.Repository;

@Repository
public interface LoanTrackerRepository {//extends JpaRepository<LoanTracker, Long> {
  //  List<LoanTracker> findByUserId(Long userId);
}
